# App-specific R8 rules.
#
# The ARCore / EGL / MediaCodec / sensor keeps that used to live here moved to
# packages/stera_recorder/android/consumer-rules.pro, which the plugin applies
# to every consumer automatically.
