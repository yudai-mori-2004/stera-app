# Monorepo/turbo shim — not part of the published package.
package.json

# The example's runner projects are regenerable and add nothing for a reader
# browsing the package on pub.dev. example/lib and example/pubspec.yaml stay.
example/android/
example/ios/
example/.metadata
